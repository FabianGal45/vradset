<div class="container">
  <div class="row text-center">
      <h1>Advertisements</h1>
  </div>

  <%if user_advertiser_or_admin? %>
    <div class="row justify-content-center my-3">
        <div class="container text-center">
          <%= link_to "New advertisement", new_advertisement_path, class:"btn btn-primary", role:"button" %>
        </div>
    </div>
  <% end %>

  <div id="advertisements" class="row justify-content-center row-cols-md-3 g-3">
    <% @advertisements.each do |advertisement| %>
      <div class="col">
        <div class="card text-center">
          <div class="card-body">
            <%= render advertisement %> 
            <% if user_advertiser_or_admin? %>
              <%= link_to "Show this advertisement", advertisement, class:"mt-3 btn btn-outline-secondary", role:"button"  %>
            <% end %>  
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>